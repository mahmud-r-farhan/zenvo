package com.bengalbytes.zenvo.ui.theme

import androidx.compose.ui.graphics.Color

// ── Core Dark Palette ──
val DarkBackground = Color(0xFF0B0F1A)
val DarkSurface = Color(0x801E293B) // Glassmorphic alpha
val SolidSurface = Color(0xFF151D2E)
val CardSurface = Color(0xFF1A2336)
val ElevatedSurface = Color(0xFF1F2B3E)

// ── Accent Colors ──
val PrimaryAccent = Color(0xFF38BDF8)    // Sky blue
val SecondaryAccent = Color(0xFF818CF8)  // Indigo
val TertiaryAccent = Color(0xFF34D399)   // Emerald

// ── Gradient Colors ──
val GradientStart = Color(0xFF38BDF8)
val GradientEnd = Color(0xFF818CF8)
val WarmGradientStart = Color(0xFFF472B6) // Pink
val WarmGradientEnd = Color(0xFFFBBF24)   // Amber

// ── Semantic Colors ──
val SuccessGreen = Color(0xFF34D399)
val WarningAmber = Color(0xFFFBBF24)
val ErrorRed = Color(0xFFF87171)
val InfoBlue = Color(0xFF60A5FA)

// ── Text Colors ──
val TextPrimary = Color(0xFFF8FAFC)
val TextSecondary = Color(0xFF94A3B8)
val TextMuted = Color(0xFF64748B)

// ── Border & Divider ──
val GlassBorder = Color(0x1AFFFFFF) // ~10% white
val Divider = Color(0x33FFFFFF)      // ~20% white

// ── Timer-specific colors ──
val PomodoroColor = Color(0xFF38BDF8)   // Sky blue for focus
val ShortBreakColor = Color(0xFF34D399) // Green for short break
val LongBreakColor = Color(0xFF818CF8)  // Indigo for long break
